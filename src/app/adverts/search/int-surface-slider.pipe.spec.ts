import { IntSurfaceSliderPipe } from './int-surface-slider.pipe';

describe('IntSurfaceSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new IntSurfaceSliderPipe();
    expect(pipe).toBeTruthy();
  });
});
