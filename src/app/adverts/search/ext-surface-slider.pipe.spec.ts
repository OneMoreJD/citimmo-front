import { ExtSurfaceSliderPipe } from './ext-surface-slider.pipe';

describe('ExtSurfaceSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtSurfaceSliderPipe();
    expect(pipe).toBeTruthy();
  });
});
